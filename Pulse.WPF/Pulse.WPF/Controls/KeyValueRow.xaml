<!--
    KeyValueRow — two-column key/value row used inside cards.
    Bind:
        <controls:KeyValueRow Label="IP:" Value="{Binding Ip}" IsMono="True" />
    Set ValueColor to override the value brush (e.g. yellow for errors).
-->
<UserControl x:Class="Pulse.WPF.Controls.KeyValueRow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Name="Root">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="{Binding ElementName=Root, Path=LabelWidth}" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Column="0"
                   Text="{Binding ElementName=Root, Path=Label}"
                   Style="{StaticResource MetaSm}"
                   VerticalAlignment="Center" />

        <TextBlock x:Name="ValueTextBlock"
                   Grid.Column="1"
                   Text="{Binding ElementName=Root, Path=Value}"
                   Foreground="{Binding ElementName=Root, Path=ValueColor}"
                   VerticalAlignment="Center" />
    </Grid>
</UserControl>
