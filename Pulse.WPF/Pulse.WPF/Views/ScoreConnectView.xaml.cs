using System.Windows.Controls;

namespace Pulse.WPF.Views
{
    /// <summary>
    /// Code-behind for the Score Connect panel. Kept minimal — the panel's
    /// behaviour lives entirely on <see cref="ViewModels.ScoreConnectViewModel"/>.
    /// </summary>
    public partial class ScoreConnectView : UserControl
    {
        public ScoreConnectView()
        {
            InitializeComponent();
        }
    }
}
